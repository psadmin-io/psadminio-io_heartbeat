- type: http
  schedule: '@every 60s'
  id: web-<%= @domain_name %>-<%= @hostname %>
  name: <%= @domain_name %>-<%= @hostname %>
  service.name: <%= service_name %>
  urls: ["http://<%= fqdn %>:<%= pia_http_port %>/index.html"]
  check.response:
    status: [200, 503]