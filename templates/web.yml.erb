- type: http
  schedule: '@every <%= @check_interval %>'
  id: web-<%= @domain_name %>-<%= @hostname %>
  name: <%= @domain_name %>-<%= @hostname %>
  service.name: <%= @service_name %>
  urls: ["http://<%= @fqdn %>:<%= @web_port %>/index.html"]
  check.response:
    status: [200, 503]