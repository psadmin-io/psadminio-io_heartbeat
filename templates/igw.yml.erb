- type: http
  schedule: '@every <%= @check_interval %>'
  id: igw-<%= @service_name%>
  name: igw-<%= @service_name%>
  service.name: <%= @service_name%>
  urls: [ "<%= @igw_url %>" ]
  check.response:
    body: ACTIVE